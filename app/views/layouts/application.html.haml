!!!
%html
%head
  %title Lede.in
  = stylesheet_link_tag "application", :media => "all"
  = javascript_include_tag "application"
  = csrf_meta_tags

%body.preview

- if current_user
  .navbar.navbar-fixed-top
    .navbar-inner
      .container
        %a= link_to "lede.in", root_path, class: "brand"
        %ul.nav.pull-right
          %li.dropdown
            = link_to "#", data: {toggle: "dropdown"}, class: "dropdown-toggle" do
              = current_user.x_auth_username
              %b.caret
            %ul.dropdown-menu
              %li
                = link_to "Profile", user_path(current_user)
              %li.divider
              %li
                = link_to "Sign out", signout_path

.container
  = yield

.footer{:style => "padding: 0 20% 0 20%; margin-top: 60px;"}
  = debug(params) if Rails.env.development?